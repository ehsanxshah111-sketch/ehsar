// The admin login is intentionally NOT linked from anywhere in the public site
// (no footer/header link) so it stays hidden from normal visitors.
// Change this path to something unique before you deploy, and keep it private.
export const ADMIN_LOGIN_PATH = "/ehsar-control-x7q9";
export const ADMIN_DASHBOARD_PATH = "/ehsar-control-x7q9/dashboard";
